
function PlaceholderBox() {
  return (
    <div
      style={{
        width: "100%",
        height: "240px",
        backgroundColor: "#ccc",
        borderRadius: "8px",
        cursor: "pointer",
        transition: "transform 0.2s, backgroundColor 0.2s"
      }}
    />
  );
}

export default PlaceholderBox;